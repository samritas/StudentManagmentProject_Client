// reducers/index.js

import { combineReducers } from 'redux';
// import counterReducer from '../Redcer/Auth/reducer';

const rootReducer = combineReducers({
  // counter: counterReducer,
  // Add more reducers here as needed
});

export default rootReducer;
